import { useState } from 'react'


function App() {
  const username = "AYUSH"  // let's see how js can be use iin html 

  return ( 
     <h1> hello world | {username}</h1> //  here we use js in html but some point to remember 
                          // {inside this treat it as variable} , 
                          // now this called evaluated expression means final outcome of js will use here 
                          // if we use if(){} something then give error 
                          

  )
}

export default App
